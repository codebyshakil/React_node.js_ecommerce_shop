# 🛒 EW Shop - E-Commerce Platform

একটি ফুল-ফিচার্ড ই-কমার্স প্ল্যাটফর্ম যা React, Vite, Tailwind CSS, এবং Supabase দিয়ে তৈরি। ওয়ার্ডপ্রেসের মতো এক-ক্লিকে ইনস্টল করে যেকোনো Supabase প্রজেক্টে নিজের অনলাইন স্টোর চালু করুন।

---

## 🚀 ফিচারসমূহ

- ✅ ওয়ান-ক্লিক ইনস্টলেশন উইজার্ড (`/install`)
- ✅ সম্পূর্ণ অ্যাডমিন প্যানেল (প্রোডাক্ট, অর্ডার, কাস্টমার, ব্লগ, পেজ ম্যানেজমেন্ট)
- ✅ মাল্টিপল পেমেন্ট গেটওয়ে (SSLCommerz, bKash, Nagad, Stripe, PayPal, COD)
- ✅ Row Level Security (RLS) সহ সুরক্ষিত ডেটাবেস
- ✅ রেসপন্সিভ ডিজাইন (মোবাইল + ডেস্কটপ)
- ✅ SEO অপটিমাইজড
- ✅ ইমেইল মার্কেটিং ক্যাম্পেইন
- ✅ কুপন ও শিপিং ম্যানেজমেন্ট
- ✅ রোল-বেসড পারমিশন সিস্টেম

---

## 📋 প্রয়োজনীয়তা (Prerequisites)

শুরু করার আগে নিশ্চিত করুন আপনার কাছে আছে:

1. **Node.js** (v18+) — [ডাউনলোড করুন](https://nodejs.org/)
2. **npm** বা **bun** — Node.js এর সাথে npm আসে
3. **Git** — [ডাউনলোড করুন](https://git-scm.com/)
4. **Supabase CLI** — [ইনস্টল গাইড](https://supabase.com/docs/guides/cli/getting-started)
5. **Supabase অ্যাকাউন্ট** — [ফ্রি অ্যাকাউন্ট খুলুন](https://supabase.com/)

---

## 🛠️ সম্পূর্ণ ডিপ্লয়মেন্ট গাইড (Step-by-Step)

### ধাপ ১: GitHub থেকে কোড ক্লোন করুন

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### ধাপ ২: Dependencies ইনস্টল করুন

```bash
npm install
```

### ধাপ ৩: নতুন Supabase প্রজেক্ট তৈরি করুন

1. [Supabase Dashboard](https://supabase.com/dashboard) এ যান
2. **"New Project"** বাটনে ক্লিক করুন
3. প্রজেক্টের নাম দিন (যেমন: `my-store`)
4. একটি শক্তিশালী **Database Password** দিন — এটি সংরক্ষণ করুন, পরে লাগবে!
5. আপনার কাছের **Region** সিলেক্ট করুন
6. **"Create new project"** ক্লিক করুন
7. প্রজেক্ট তৈরি হতে ১-২ মিনিট অপেক্ষা করুন

### ধাপ ৪: Supabase Credentials সংগ্রহ করুন

Supabase Dashboard → **Settings** → **API** থেকে:

| তথ্য | কোথায় পাবেন |
|---|---|
| **Project URL** | `Settings → API → Project URL` |
| **Anon Key** | `Settings → API → anon public` |
| **Database URL** | `Settings → Database → Connection string → URI` (পাসওয়ার্ড বসিয়ে দিন) |

### ধাপ ৫: `.env` ফাইল তৈরি/আপডেট করুন

প্রজেক্টের রুটে `.env` ফাইল তৈরি করুন:

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6...আপনার_anon_key
VITE_SUPABASE_PROJECT_ID=YOUR_PROJECT_ID
```

> ⚠️ **সতর্কতা:** `.env` ফাইল কখনো GitHub এ পুশ করবেন না! `.gitignore` এ `.env` যোগ আছে কিনা নিশ্চিত করুন।

### ধাপ ৬: Supabase CLI লিংক করুন

```bash
# Supabase CLI তে লগইন করুন
npx supabase login

# আপনার Supabase প্রজেক্টের সাথে লিংক করুন
npx supabase link --project-ref YOUR_PROJECT_ID
```

`YOUR_PROJECT_ID` আপনার Supabase Dashboard URL থেকে পাবেন:
`https://supabase.com/dashboard/project/YOUR_PROJECT_ID`

### ধাপ ৭: Edge Functions এর জন্য Secret সেট করুন

Supabase Dashboard → **Settings** → **Edge Functions** → **Secrets** এ যান এবং নিচের secret যোগ করুন:

| Secret Name | মান |
|---|---|
| `SUPABASE_DB_URL` | আপনার Database Connection String (URI ফরম্যাট) |

**Database URL কোথায় পাবেন:**
1. Supabase Dashboard → **Settings** → **Database**
2. **Connection string** সেকশনে → **URI** ট্যাব
3. URL কপি করুন এবং `[YOUR-PASSWORD]` এর জায়গায় আপনার ডেটাবেস পাসওয়ার্ড বসান

উদাহরণ:
```
postgresql://postgres.xxxx:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres
```

> 💡 এই সিক্রেটটি `initial-setup` edge function ব্যবহার করে ডেটাবেস টেবিল তৈরি করতে।

### ধাপ ৮: Edge Functions ডিপ্লয় করুন

```bash
npx supabase functions deploy
```

এই কমান্ড সব edge functions একসাথে ডিপ্লয় করবে:
- `initial-setup` — ইনস্টলেশন উইজার্ড
- `create-employee` — এমপ্লয়ি ম্যানেজমেন্ট
- `send-notification` — নোটিফিকেশন
- `sslcommerz-init`, `bkash-init`, `stripe-create-checkout` ইত্যাদি — পেমেন্ট গেটওয়ে

> 💡 আলাদা করে ডিপ্লয় করতে চাইলে: `npx supabase functions deploy initial-setup`

### ধাপ ৯: লোকাল ডেভেলপমেন্ট সার্ভার চালু করুন

```bash
npm run dev
```

ব্রাউজারে `http://localhost:5173` ওপেন হবে।

### ধাপ ১০: ইনস্টলেশন সম্পন্ন করুন

1. ব্রাউজারে `/install` পেজে যান
2. **Admin Name** দিন
3. **Admin Email** দিন
4. **Admin Password** দিন (কমপক্ষে ৬ অক্ষর)
5. **"Install Now"** বাটনে ক্লিক করুন

ইনস্টলেশন প্রক্রিয়ায় স্বয়ংক্রিয়ভাবে:
- ✅ ২৫+ ডেটাবেস টেবিল তৈরি হবে
- ✅ ১০০+ RLS সিকিউরিটি পলিসি সেটআপ হবে
- ✅ অ্যাডমিন অ্যাকাউন্ট তৈরি হবে
- ✅ ডিফল্ট সেটিংস কনফিগার হবে

### ধাপ ১১: অ্যাডমিন প্যানেলে লগইন করুন

ইনস্টলেশন শেষে `/admin` পেজে রিডাইরেক্ট হবে। আপনার অ্যাডমিন ইমেইল ও পাসওয়ার্ড দিয়ে লগইন করুন।

অ্যাডমিন প্যানেল থেকে কনফিগার করুন:
- 🏪 সাইটের নাম, লোগো, বর্ণনা
- 💰 কারেন্সি সেটিংস
- 💳 পেমেন্ট মেথড (bKash, Nagad, SSLCommerz, Stripe, PayPal)
- 📧 SMTP/ইমেইল সেটিংস
- 🚚 শিপিং জোন ও রেট

---

## 🌐 লাইভ ডিপ্লয়মেন্ট (Hosting)

### অপশন ১: Vercel (সবচেয়ে সহজ)

```bash
# Vercel CLI ইনস্টল
npm i -g vercel

# ডিপ্লয়
vercel
```

Vercel Dashboard এ **Environment Variables** যোগ করুন:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`
- `VITE_SUPABASE_PROJECT_ID`

### অপশন ২: Netlify

```bash
npm run build
# dist/ ফোল্ডারটি Netlify তে ড্র্যাগ অ্যান্ড ড্রপ করুন
```

### অপশন ৩: যেকোনো স্ট্যাটিক হোস্টিং

```bash
npm run build
```

`dist/` ফোল্ডারের ফাইলগুলো আপনার হোস্টিং এ আপলোড করুন। SPA রাউটিং এর জন্য সব রিকোয়েস্ট `index.html` এ রিডাইরেক্ট করতে হবে।

> ⚠️ **গুরুত্বপূর্ণ:** হোস্টিং এ environment variables সেট করতে ভুলবেন না!

---

## ❓ সাধারণ সমস্যা ও সমাধান

### ❌ "Installation Failed" এরর
- ✅ `SUPABASE_DB_URL` সিক্রেট সঠিকভাবে সেট হয়েছে কিনা চেক করুন
- ✅ ডেটাবেস পাসওয়ার্ড সঠিক কিনা নিশ্চিত করুন
- ✅ Edge functions ডিপ্লয় হয়েছে কিনা দেখুন

### ❌ "Edge Function not found" এরর
- ✅ `npx supabase functions deploy` চালান
- ✅ Supabase CLI সঠিক প্রজেক্টে লিংক হয়েছে কিনা চেক করুন

### ❌ পেজ লোড হচ্ছে না / ব্ল্যাংক পেজ
- ✅ `.env` ফাইলের ভ্যালু সঠিক কিনা চেক করুন
- ✅ ব্রাউজার কনসোলে এরর দেখুন (F12)

### ❌ লগইন কাজ করছে না
- ✅ ইনস্টলেশন সম্পন্ন হয়েছে কিনা নিশ্চিত করুন
- ✅ সঠিক ইমেইল ও পাসওয়ার্ড ব্যবহার করছেন কিনা দেখুন

---

## 🧰 টেকনোলজি স্ট্যাক

- **Frontend:** React 18, TypeScript, Vite
- **UI:** Tailwind CSS, shadcn/ui, Framer Motion
- **Backend:** Supabase (PostgreSQL, Auth, Edge Functions, Storage)
- **State Management:** TanStack React Query
- **Routing:** React Router DOM

---

## 📄 লাইসেন্স

এই প্রজেক্টটি ওপেন সোর্স। আপনি চাইলে এটি ব্যবহার, পরিবর্তন এবং বিতরণ করতে পারেন।

---

## 🤝 সাহায্য দরকার?

কোনো সমস্যা হলে GitHub Issues এ জানান অথবা আমাদের সাথে যোগাযোগ করুন।
