# 🛒 EW Shop - E-Commerce Platform

একটি ফুল-ফিচার্ড ই-কমার্স প্ল্যাটফর্ম যা React, Vite, Tailwind CSS, এবং Supabase দিয়ে তৈরি।

---

## 🚀 Quick Start (৩ ধাপে সেটআপ)

### ধাপ ১: কোড ক্লোন ও ডিপেন্ডেন্সি ইনস্টল

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
npm install
```

### ধাপ ২: Supabase প্রজেক্ট তৈরি ও কনফিগার

1. [Supabase Dashboard](https://supabase.com/dashboard) এ গিয়ে **"New Project"** তৈরি করুন
2. প্রজেক্ট তৈরি হলে **Settings → API** থেকে নিচের তথ্য সংগ্রহ করুন:

| তথ্য | কোথায় পাবেন |
|---|---|
| **Project URL** | Settings → API → Project URL |
| **Anon Key** | Settings → API → anon public |
| **Project ID** | URL থেকে: `https://supabase.com/dashboard/project/PROJECT_ID` |

3. প্রজেক্টের রুটে `.env` ফাইল তৈরি করুন:

```env
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here
VITE_SUPABASE_PROJECT_ID=YOUR_PROJECT_ID
```

> ⚠️ `.env` ফাইল কখনো GitHub এ পুশ করবেন না!

### ধাপ ৩: Edge Functions ডিপ্লয়

```bash
# Supabase CLI তে লগইন
npx supabase login

# প্রজেক্ট লিংক করুন
npx supabase link --project-ref YOUR_PROJECT_ID

# সব Edge Functions একসাথে ডিপ্লয়
npx supabase functions deploy
```

> 💡 `YOUR_PROJECT_ID` আপনার Supabase Dashboard URL থেকে পাবেন।

---

## 🎯 ইনস্টলেশন উইজার্ড

Edge Functions ডিপ্লয় হয়ে গেলে, বাকি সব কিছু **অটোমেটিক**!

```bash
npm run dev
```

ব্রাউজারে `http://localhost:5173/install` ওপেন করুন। ইনস্টলেশন উইজার্ড ৩টি ধাপে কাজ করে:

### Step 1: Database URL দিন
- **Supabase Dashboard → Settings → Database → Connection string → URI** থেকে কপি করুন
- `[YOUR-PASSWORD]` এর জায়গায় আপনার ডেটাবেস পাসওয়ার্ড বসান
- উদাহরণ: `postgresql://postgres.xxxx:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres`

### Step 2: Admin Account তৈরি
- নাম, ইমেইল, পাসওয়ার্ড দিন (পাসওয়ার্ড কমপক্ষে ৬ অক্ষর)

### Step 3: Install Now ক্লিক করুন
ইনস্টলেশন স্বয়ংক্রিয়ভাবে করবে:
- ✅ ২৫+ ডেটাবেস টেবিল তৈরি
- ✅ ১০০+ RLS সিকিউরিটি পলিসি সেটআপ
- ✅ অ্যাডমিন অ্যাকাউন্ট তৈরি
- ✅ ডিফল্ট সেটিংস কনফিগার

ইনস্টলেশন শেষে `/admin` এ রিডাইরেক্ট হবে। অ্যাডমিন প্যানেল থেকে কনফিগার করুন:
- 🏪 সাইটের নাম, লোগো
- 💰 কারেন্সি সেটিংস
- 💳 পেমেন্ট মেথড (bKash, Nagad, SSLCommerz, Stripe, PayPal, COD)
- 📧 SMTP/ইমেইল সেটিংস
- 🚚 শিপিং জোন ও রেট

---

## 🌐 লাইভ ডিপ্লয়মেন্ট

### Vercel (সবচেয়ে সহজ)

```bash
npm i -g vercel
vercel
```

Vercel Dashboard এ Environment Variables যোগ করুন:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`
- `VITE_SUPABASE_PROJECT_ID`

### অন্যান্য হোস্টিং

```bash
npm run build
```

`dist/` ফোল্ডার আপলোড করুন। SPA রাউটিং এর জন্য সব রিকোয়েস্ট `index.html` এ রিডাইরেক্ট করুন।

---

## ❓ সমস্যা ও সমাধান

| সমস্যা | সমাধান |
|---|---|
| Installation Failed | Database URL সঠিক কিনা ও পাসওয়ার্ড ঠিক আছে কিনা চেক করুন |
| Edge Function not found | `npx supabase functions deploy` চালান |
| ব্ল্যাংক পেজ | `.env` ভ্যালু সঠিক কিনা ও ব্রাউজার কনসোল (F12) চেক করুন |
| লগইন কাজ করছে না | ইনস্টলেশন সম্পন্ন হয়েছে কিনা নিশ্চিত করুন |

---

## 🧰 টেকনোলজি স্ট্যাক

- **Frontend:** React 18, TypeScript, Vite, Tailwind CSS, shadcn/ui
- **Backend:** Supabase (PostgreSQL, Auth, Edge Functions, Storage)
- **State:** TanStack React Query
- **Routing:** React Router DOM

---

## ✨ ফিচারসমূহ

- ✅ ওয়ান-ক্লিক ইনস্টলেশন উইজার্ড
- ✅ সম্পূর্ণ অ্যাডমিন প্যানেল
- ✅ মাল্টিপল পেমেন্ট গেটওয়ে
- ✅ RLS সিকিউরিটি
- ✅ রেসপন্সিভ ডিজাইন
- ✅ SEO অপটিমাইজড
- ✅ ইমেইল মার্কেটিং
- ✅ কুপন ও শিপিং ম্যানেজমেন্ট
- ✅ রোল-বেসড পারমিশন সিস্টেম

---

## 📄 লাইসেন্স

এই প্রজেক্টটি ওপেন সোর্স।
